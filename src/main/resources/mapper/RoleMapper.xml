<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.mapper.RoleMapper">

<!--    <select id="getRoleListByUserId" resultType="com.system.entity.Role" parameterType="java.lang.Long">-->
<!--        select r.* from sys_role as r left join sys_user_role as ur on ur.role_id=r.id where id=#{userId}-->
<!--    </select>-->
</mapper>
